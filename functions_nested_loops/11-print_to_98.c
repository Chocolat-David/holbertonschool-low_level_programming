#include <stdio.h>
#include <main.h>

int main(void)
{


	int i = 0;
	for (i ; i < 99 ; i++)
	{
		if (n != 0)
			{
			putchar(',');
			putchar(' ');
			}
		printf("%d", i);
	}
	putchar('\n');
return (0);
}
